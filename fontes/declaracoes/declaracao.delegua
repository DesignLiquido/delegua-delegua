classe Declaracao {
    _tipo: texto
    _valor: qualquer

    construtor(_tipo: texto, _valor: qualquer) {
        isto._tipo = _tipo
        isto._valor = _valor
    }

    paraTexto(): texto {
        retorna "Declaracao(" + isto._tipo + ")"
    }
}
