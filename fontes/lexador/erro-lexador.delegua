classe ErroLexador {
    mensagem: texto
    linha: número

    construtor(mensagem: texto, linha: número) {
        isto.mensagem = mensagem
        isto.linha = linha
    }

    paraTexto(): texto {
        var mensagem: texto = isto.mensagem
        retorna "<erro-lexador mensagem=${mensagem} />"
    }
}
